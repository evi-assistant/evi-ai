{
  "name": "evi-desktop",
  "private": true,
  "version": "0.1.0",
  "description": "Tauri shell that launches the Evi local web UI as a desktop app.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
